name: Environment Based Tests

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "qa"
        type: choice
        options:
          - qa
          - prod
      suite:
        description: "Test suite to run"
        required: true
        default: "smoke"
        type: choice
        options:
          - smoke
          - regression

jobs:
  run-tests:
    runs-on: ubuntu-latest

    env:
      ENV: ${{ github.event.inputs.environment }}
      SUITE: ${{ github.event.inputs.suite }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Print context
        run: |
          echo "Running $SUITE tests on $ENV environment"

      - name: Run tests
        run: |
          if [ "$SUITE" = "smoke" ]; then
            mvn clean test -Dsurefire.suiteXmlFiles=testng-smoke.xml -Denv=$ENV
          else
            mvn clean test -Dsurefire.suiteXmlFiles=testng-regression.xml -Denv=$ENV
          fi

      - name: Upload report (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: target/surefire-reports
